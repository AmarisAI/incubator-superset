apiVersion: v1
kind: Service
metadata:
  name: {{ template "amaris.fullname" . }}
  labels:
    app: {{ template "amaris.name" . }}
    chart: {{ template "amaris.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "amaris.name" . }}
    release: {{ .Release.Name }}
